{% if not isTimePaused %}
{% set actor_name = decnpc(npc.UUID).name %}
{% if triggeringEvent and triggeringEvent.type == "direct_narration" %}
{% else %}
    {# overriding this in the binding prompt - if before binding in load order #}
{% endif %}
{% endif %}